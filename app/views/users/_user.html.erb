<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("static_pages.del"), user, method: :delete,
                  data: {confirm: t("static_pages.cofirm_del")} %>
  <% end %>
</li>
