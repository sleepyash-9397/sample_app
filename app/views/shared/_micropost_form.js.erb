$('#micropost_picture').bind('change', function() {
  var size_in_megabytes = this.files[0].size/1024/1024;
  if (size_in_megabytes >  <%= Settings.size_picture %>) {
    alert('<%= t("js.alert") +
    "#{Settings.size_picture}" + " MB. " +
    t("js.alert_continue") %>');
  }
});
